# Factory On Call — Demo Emergency + Light Default Patch

Updated demo onboarding defaults:

- Demo plants now seed Emergency settings with the station emergency button enabled.
- Emergency is inactive by default, but ready to test from station screens.
- Demo/pro plant branding now defaults to Light mode.
- App shell fallback theme defaults changed from Dark to Light for Admin, Station, Supervisor, Viewer, and Display.

This keeps the demo easier to learn while still allowing customers to test the emergency workflow.
