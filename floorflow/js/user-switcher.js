import { requireRoleAccess } from './auth-lock.js';
export async function mountUserSwitcher({selectId='userSwitcher'}={}){const select=document.getElementById(selectId);if(!select)return;const button=document.createElement('button');button.type='button';button.id=selectId;button.className=select.className;button.textContent='Switch user';select.replaceWith(button);button.onclick=async()=>{await requireRoleAccess(undefined,true);location.reload();};}
